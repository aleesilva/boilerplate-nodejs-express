/**
 * the service file is responsible for
 * handling all operations of calls to
 * external api's for that module
 */

const service = {
  /**
   *
   * adding service methods
   *  */
}

module.exports = service
